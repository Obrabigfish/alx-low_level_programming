#include "main.h"
/**
 *  _isupper - checks for uppercase
 * @c: variable to be checked
 * Return: 1 if uppercase else 0
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
